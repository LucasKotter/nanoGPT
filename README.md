
# nanoGPT

## install

```
pip install torch numpy transformers datasets tiktoken wandb tqdm
```

## quick start

Use the ```project.ipynb``` to run the models

Be careful to not delete previous files while doing so


